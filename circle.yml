machine:
  ghc:
    version: 9.0.2

dependencies:
  override:
    - cabal update
    - cabal sandbox init
    - cabal install --only-dependencies -j
    - cabal configure

test:
  override:
    - cabal build
